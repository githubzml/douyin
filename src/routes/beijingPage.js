import React, { Component } from 'react';
import { NavLink } from "dva/router";

import RouterView from '../router/routerView';

export default class beijingPage extends Component {
  render() {
    return (
      <div>
          this is beijingPage
      </div>
    )
  }
}
